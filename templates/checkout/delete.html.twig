<form method="post" action="{{ path('remove_facture', {'id': f.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ f.id) }}">
    <button class="btn">Delete</button>
</form>